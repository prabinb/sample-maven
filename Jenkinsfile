pipeline{
   agent{
      label 'instance-1'
   }
   stages{
      stage('git cloning'){
         steps{
            checkout scm 
         }
      }

      stage('maven build'){

      }
   }
}
